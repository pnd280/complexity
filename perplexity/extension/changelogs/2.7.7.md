## Bug Fixes

- `Better Language Model Selector`: Fixed a bug where the model selector gone missing after routing to a new page on the Comet Assistant sidecar
- `Custom Themes`: Fixed an issue where the accent color selector was not being updated
- Fixed inconsistent behavior of "Save as file" functionality in the settings page and `Thread Export` plugin

## Improvements

- Improved the extension's initial loading time
